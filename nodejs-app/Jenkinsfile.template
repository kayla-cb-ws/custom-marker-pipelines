pipeline {
    agent any
    options {
        buildDiscarder(logRotator(numToKeepStr: '2'))
    }
    stages {
        stage('Hello World') {
            steps {
                echo 'Hello World!'
                sh 'java -version'
            }
        }
    }
}
